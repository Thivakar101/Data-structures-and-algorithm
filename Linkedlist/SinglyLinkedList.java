public class SinglyLinkedList {    private Node first, last;     private int noofnodes;    public SinglyLinkedList() {        this.first = null;        last = null;    }    public void add(int val) {        Node newNode = new Node(val);        if (first == null) {            first = newNode;            last = newNode;            noofnodes++;        } else {            last.next = newNode;            last = newNode;            noofnodes++;        }    }    public void print() {        Node temp = first;        while (temp != null) {            System.out.print(temp.val+" ");            temp = temp.next;        }    }    public void reverseprint() {        Node temp = last;        while (temp != null) {            System.out.println(temp.val);            temp = temp.next;        }    }    public void ArrayToList(int[] arr) {        for (int i = 0; i < arr.length; i++) {            add(arr[i]);            noofnodes++;        }    }    public void StringToList(String str, char delimiter) {        String[] arr = str.split(String.valueOf(delimiter));        for (int i = 0; i < arr.length; i++) {            add(Integer.parseInt(arr[i]));        }    }    public void insertback(int val){        add(val);    }    public void insertfront(int val){        Node newNode= new Node(val);        if(first==null){            first=newNode;            last=newNode;            noofnodes++;        }        else{            newNode.next=first;            first=newNode;            noofnodes++;        }    }    public void add(int val,int position){    }    public void  beforelast() {        Node temp = first;        while (temp.next != last) {            temp = temp.next;        }    }    // Delete first node    public void deletefront() {        if (first == null) return;        first = first.next;        if (first == null) last = null; // list became empty        noofnodes--;    }    public void deletelast(){        if(first==null) return ;        last=null;    }}////public class SLLRotation {////    Node head;////    class Node{//        int data;//        Node next;////        Node(int data){//            this.data = data;//            next = null;//        }//    }////    void add(int data){////        Node newNode = new Node(data);////        if(head == null){//            head = newNode;//            return;//        }////        Node temp = head;////        while(temp.next != null){//            temp = temp.next;//        }////        temp.next = newNode;//    }////    // LEFT ROTATION//    void leftRotate(int k){////        if(head == null) return;////        Node current = head;//        int count = 1;////        while(count < k && current != null){//            current = current.next;//            count++;//        }////        if(current == null) return;////        Node kthNode = current;////        while(current.next != null){//            current = current.next;//        }////        current.next = head;////        head = kthNode.next;////        kthNode.next = null;//    }////    // RIGHT ROTATION//    void rightRotate(int k){////        if(head == null) return;////        Node temp = head;//        int length = 1;////        while(temp.next != null){//            temp = temp.next;//            length++;//        }////        temp.next = head;////        k = k % length;//        int steps = length - k;////        temp = head;////        for(int i=1;i<steps;i++){//            temp = temp.next;//        }////        head = temp.next;//        temp.next = null;//    }////    void display(){////        Node temp = head;////        while(temp != null){//            System.out.print(temp.data + " ");//            temp = temp.next;//        }////        System.out.println();//    }////    public static void main(String[] args){////        SLLRotation list = new SLLRotation();////        list.add(1);//        list.add(2);//        list.add(3);//        list.add(4);//        list.add(5);////        list.leftRotate(2);//        System.out.println("Left Rotation:");//        list.display();////        list.rightRotate(2);//        System.out.println("Right Rotation:");//        list.display();//    }//}////public class DLLRotation {////    Node head;////    class Node{//        int data;//        Node next, prev;////        Node(int data){//            this.data = data;//            next = prev = null;//        }//    }////    void add(int data){////        Node newNode = new Node(data);////        if(head == null){//            head = newNode;//            return;//        }////        Node temp = head;////        while(temp.next != null){//            temp = temp.next;//        }////        temp.next = newNode;//        newNode.prev = temp;//    }////    // LEFT ROTATION//    void leftRotate(int k){////        Node current = head;//        int count = 1;////        while(count < k && current != null){//            current = current.next;//            count++;//        }////        if(current == null) return;////        Node kthNode = current;////        while(current.next != null){//            current = current.next;//        }////        current.next = head;//        head.prev = current;////        head = kthNode.next;//        head.prev = null;////        kthNode.next = null;//    }////    // RIGHT ROTATION//    void rightRotate(int k){////        Node temp = head;//        int length = 0;////        while(temp != null){//            temp = temp.next;//            length++;//        }////        leftRotate(length - k);//    }////    void display(){////        Node temp = head;////        while(temp != null){//            System.out.print(temp.data + " ");//            temp = temp.next;//        }////        System.out.println();//    }////    public static void main(String[] args){////        DLLRotation list = new DLLRotation();////        list.add(10);//        list.add(20);//        list.add(30);//        list.add(40);//        list.add(50);////        list.leftRotate(2);//        System.out.println("Left Rotation:");//        list.display();////        list.rightRotate(2);//        System.out.println("Right Rotation:");//        list.display();//    }}